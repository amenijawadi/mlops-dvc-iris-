[core]
    remote = storage
['remote "storage"']
    url = s3://mlops-dvc
    endpointurl = http://localhost:9000
    use_ssl = false
    region = us-east-1
    access_key_id = minio
    secret_access_key = minio12345
